---
EnableDefaultLinters: true
linters:
  ErbSafety:
    enabled: false
    better_html_config: .better-html.yml
  Rubocop:
    enabled: true
    rubocop_config:
      inherit_from:
        - .rubocop.yml
