<div class="error">
  <%= error_messages_for :backup_quota %>
</div>

<%= form_for([:admin, @backup_quota], url: admin_backup_quota_path) do |f| %>
<table class="entry_form edit_backup_quota" align="center">
  <caption>Edit Backup Quota</caption>
  <tfoot>
    <tr>
      <td></td>
      <td colspan="1">
        <input type='submit' value="Save changes" />
      </td>
    </tr>
  </tfoot>
  <%= render :partial => 'form', :locals => { :f => f } %>
</table>
<% end %>

&laquo; <%= link_to('Back to All Backup Quotas', admin_backup_quotas_path) %>
