<%= @account.display_name %>,

Your server has finished provisioning.

Operating System: <%= @vps.os %>

IPv4 Address: <%= @vps.display_ip_address %>
Location: <%= @vps.location.display_name_simple %>
RAM: <%= @vps.ram %> MB
Storage: <%= @vps.storage %> GB SSD

<% unless @vps.ssh_host_keys.empty? -%>
SSH Host Key Fingerprints:
<% @vps.ssh_host_keys.each do |key| -%><%= key.display_fingerprint_sha256 + "\n" %><% end %>
<% @vps.ssh_host_keys.each do |key| -%><%= key.display_fingerprint_md5    + "\n" %><% end %>
<% end -%>
You may login to your server using SSH with the key(s) you provided, or using the "View Console" button in the Portal.

The "Initial Logins" section under Virtual Machine Detail show your login credentials.  Please change these.

Thank you for choosing ARP Networks!

P.S. You actually *can* reply to this email. 😮

<%= render partial: 'mailer/signatures/support' %>
